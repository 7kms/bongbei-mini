<view class="container">
  <view class="list">
    <view class="item" wx:for="{{list}}" wx:key="{{item._id}}">
      <image src="{{imagePrefix + item.cover}}"  mode="aspectFill" class="image"></image>
    </view>
  </view>
</view>
